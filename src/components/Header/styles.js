import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: "center",

  },
  title: {
    fontSize: 18,
    color: "#FFF",
    fontWeight: "bold",
    marginLeft: 8,
  },
  top: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10,
  }
});